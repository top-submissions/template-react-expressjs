<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Sign Up</title>
  </head>
  <body>
    <h1>Sign Up</h1>
    
    <% if (locals.errors && errors.length > 0) { %>
      <div>
        <h3>Please fix the following errors:</h3>
        <% errors.forEach(error => { %>
          <p><%= error.msg %></p>
        <% }) %>
      </div>
    <% } %>
    
    <form action="/sign-up" method="POST">
      <div>
        <label for="username">Username</label>
        <input 
          id="username" 
          name="username" 
          placeholder="username" 
          type="text"
          value="<%= locals.formData?.username || '' %>"
        />
        <small>Username must be 3-20 characters and can contain letters, numbers, and underscores.</small>
      </div>
      
      <div>
        <label for="password">Password</label>
        <input 
          id="password" 
          name="password" 
          type="password"
        />
        <small>Password must be at least 6 characters with at least one uppercase, one lowercase, and one number.</small>
      </div>
      
      <div>
        <label for="confirmPassword">Confirm Password</label>
        <input 
          id="confirmPassword" 
          name="confirmPassword" 
          type="password"
        />
      </div>
      
      <button type="submit">Sign Up</button>
    </form>
    
    <p>Already have an account? <a href="/log-in">Log in</a></p>
  </body>
</html>